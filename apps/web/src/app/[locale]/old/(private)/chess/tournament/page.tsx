import TournamentContent from '@/components/chess/tournament-content';

export const metadata = {
  title: 'Tournament | Chess Competition Platform',
  description: 'View results from the latest tournament',
};

export default function TournamentPage() {
  return <TournamentContent />;
}
