{
  "id": 3,
  "format_ver": "1.0.0",
  "type": "code",
  "rules": ["inputs: y", "outputs: y", "chars: 1000", "files: 2"],
  "status": 3,
  "subject": ["javascript", "palindrome"],
  "title": "Palindrome Checker",
  "description": "Write a function that checks if a given string is a palindrome.",
  "initialCode": [
    "function isPalindrome(str) {\n  const cleanStr = str.toLowerCase().replace(/[^a-z0-9]/g, '');\n  return cleanStr === cleanStr.split('').reverse().join('');\n}\n\n// Do not modify the code below\nconsole.log(isPalindrome(inputs[0]));\nconsole.log(isPalindrome(inputs[1]));",
    "// This file can be used for helper functions if needed\nfunction removeNonAlphanumeric(str) {\n  return str.replace(/[^a-z0-9]/gi, '');\n}\n\nfunction reverseString(str) {\n  return str.split('').reverse().join('');\n}"
  ],
  "codeName": ["palindrome.js", "helpers.js"],
  "inputs": [
    [["racecar","hello"]]
  ],
  "outputs": [
    "true",
    "false"
  ],
  "score": [10, 0]
}

