# Economy production traceability

This matrix links the normative architecture and threat model to the executable
surface. `implemented` means the feature and its release evidence exist.
`partial` means a production-safe subset exists but the complete user or
operational surface is not yet delivered. `missing` means the accepted product
scope has no executable implementation yet.
`blocked-by-configuration` identifies external credentials, certification or
commercial authorization that source code cannot supply; it is never an implicit
permission to move value. The durable value-movement foundation is implemented.
The accepted Economy product surface is implemented. External provider
credentials, provider certification and commercial/legal authorization remain
blocked by configuration, so value movement still starts disabled and the
jurisdiction allowlist remains empty.

| Requirement | Status | Executable evidence or implementation target |
| --- | --- | --- |
| Fixed parity, append-only postings, source stamps, FIFO allocation and provenance reversals | implemented | `GameGuild.Finance.Economy` posting and PostgreSQL gateway tests, including `apps/api/tests/GameGuild.Finance.Economy.UnitTests/Persistence/PostgreSqlFifoFragmentReservationGatewayTests.cs` and `apps/api/tests/GameGuild.API.UnitTests/Database/EconomyWriterParityPostgreSqlMigrationTests.cs` |
| Risk-reserve binding, unique outbox, FIFO payout reservation, debt preservation and actor/tenant/risk/root binding | implemented | `PostgreSqlHardToSoftConversionGatewayTests`, `PostgreSqlFifoFragmentReservationGatewayTests`, `PostgreSqlProviderReversalGatewayTests` and `DurablePayoutReservationWorkflowTests` provide behavioral coverage; `EconomyWriterParityPostgreSqlMigrationTests` validates migration rollback and replay. Retain this evidence when retiring `feat/economy-ledger-hardening`. |
| Runtime uses PostgreSQL rather than process memory for entity graph, aggregate limits, cooldown, review, reserve, custody, anchors, AdRewards and Marketplace | implemented | Production DI in `EconomyCoreModule`, `AdRewardsModule`, `MarketplaceModule`, `BountiesModule`, `PayoutsModule` and `TreasuryModule` registers PostgreSQL gateways. In-memory stores remain unregistered test fixtures. PostgreSQL model and behavioral suites cover the durable stores. |
| Financial Crime, Trust/Safety and KYC inputs are versioned, expiring, auditable, durable and fail-closed | implemented | `PostgreSqlComplianceEvidenceStore`, `PostgreSqlFinancialCrimeControlPlane`, `PostgreSqlTrustSafetyControlPlane`, `SumSubKycAmlProvider` and `SumSubKycAmlOrchestrator` persist ordered evidence, holds, cases and decisions. Administrative case/appeal APIs derive tenant from actor context. SumSub credentials remain `blocked-by-configuration`. |
| Journal verification, projection recomposition and independent signed WORM anchors | implemented | `PostgreSqlJournalIntegrityService`, `PostgreSqlProjectionGenerationService`, `PostgreSqlEconomyAnchorPublisher`, `PostgreSqlAnchorVerificationService`, `AwsKmsEconomyCryptography` and `S3ObjectLockWormAnchorStore`; invalid evidence activates containment. KMS/WORM credentials remain `blocked-by-configuration`. |
| Capability composition enables Core, Bounties, Marketplace, AdRewards, Payouts and Treasury only when their predicates are valid | implemented | `ApiProductComposition` composes all six modules. `EconomyCapabilityAuthorizationService` issues and consumes signed, single-use receipts; absent policy/evidence/provider/jurisdiction remains fail-closed. |
| Versioned policies for fees, limits, reserves, cooldowns, providers and jurisdictions have no permissive default | implemented | `PostgreSqlEconomyCapabilityPolicyStore`, `PostgreSqlReserveCustodyControlPlane`, risk-counter/cooldown stores and `PostgreSqlEconomyCapabilityControlPlaneStore` bind effective signed policy, reserve, risk and kill-switch versions. No active policy means `Disabled`; the allowlist starts empty. |
| Shadow legacy migration includes provenance backfill, reconciliation, reversible cutover and read-only mode | implemented | `PostgreSqlLegacyEconomyShadowMigration`, `PostgreSqlLegacyBalanceBackfillGateway`, `PostgreSqlEconomyWalletProvisioner` and `20260825053623_AddEconomyLegacyShadowMigration*` capture tenant source snapshots, post non-zero balances through the protected writer, reconcile exact provenance and enforce a reversible three-actor cutover. `PostgreSqlLegacyEconomyShadowMigrationTests` covers replay, cross-tenant denial, unsafe-source containment, non-zero posting, append-only canonical journal metadata, cutover/rollback and upgrade from the preceding Treasury snapshot. `EconomyLegacyMigrationAdministrationController` derives tenant/actor from context under a segregated permission. |
| Self-service wallet, conversion and payout request/read/cancel routes | implemented | `apps/api/Source/GameGuild.API/Core/Controllers/EconomyWalletController.cs` and its controller tests |
| Tenant-scoped payout review with immutable reason, two distinct administrators and append-only audit | implemented | `EconomyPayoutAdministrationController`, `ReviewPayoutRequestCommand`, `20260821100000_AddTenantScopedPayoutReview*` and their unit/PostgreSQL migration tests. The tenant comes solely from actor context; the first approval enters `AwaitingSecondApproval`, and a different tenant administrator must make the final decision. |
| Provider-neutral Stripe Connect onboarding, dispatch, signed webhook, timeout ambiguity and reconciliation | implemented | `StripeConnectPayoutProvider`, `DurablePayoutApplicationService`, `PostgreSqlPayoutDispatchOutbox`, `EconomyPayoutAccountController`, `EconomyPayoutExecutionAdministrationController` and `EconomyStripeConnectWebhookController` cover connected-account onboarding/status, idempotent dispatch, signed and timestamped webhook normalization, ambiguous timeout preservation and provider reconciliation behind `IConnectPayoutProvider`. Contract tests cover duplicate/out-of-order evidence and reconciliation without a webhook. Live Stripe sandbox credentials and certification remain `blocked-by-configuration`. |
| Bounty, Marketplace, AdRewards and Treasury operational flows use durable Core posting workflows | implemented | Bounty claim/expire/reclaim and AdRewards session/proof/report APIs are composed. `CommerceOrderMarketplaceSettlementAuthority` connects the existing `Commerce.Orders/{id}:complete` command to the durable Economy settlement while the entitlement/order transition remains outbox-driven; refund restores original provenance. Tenant-scoped Treasury proposal/approval/reserve/dispatch/reconciliation APIs and durable outboxes are composed. |
| Economy administration API has independent permissions for payout review, compliance, reserves/custody, reconciliation, ledger health and kill switches | implemented | Payout review/execution/reconciliation, Financial Crime cases, Trust/Safety appeals, signed policies, reserves/custody, journal verification, projections, anchors, capability readiness, kill switches, AdRewards reports, Marketplace refunds, Treasury and reversible legacy migration use segregated permissions and actor-derived tenant. No protected request body is authoritative for tenant and no generic monetary adjustment endpoint exists. |
| Generated OpenAPI client covers the protected Economy API with a clean diff | implemented | The generated client contains Economy Core, AdRewards, Bounties, KYC, Marketplace cart/order/payment, payout/control-plane and Treasury contracts. `scripts/ci/verify-openapi-client.sh` regenerates against the composed API and rejects an uncommitted diff. |
| Cursor-paginated operational reads, detail and audit surfaces | implemented | Tenant-scoped readers and generated client operations cover policies, custody/reserve, risk and holds, ledger verification, anchors, projection generations, Marketplace settlements/refunds/outbox, AdRewards sessions/claims/reconciliation and legacy migration batches. Controllers redact sensitive values and derive tenant authority from actor context. |
| Self-service Economy web for wallet, top-ups, transfers, KYC, payouts, bounties, AdRewards and order history | implemented | Dedicated localized routes under `apps/web/src/app/[locale]/(dashboards)/workspace/economy` provide overview, wallet, top-up detail and Stripe confirmation, typed transfers, SumSub KYC, payout detail, bounty detail, deferred AdRewards and order history. Structured disabled, review, hold, pending and ambiguous states are rendered by shared Economy UI components. |
| Marketplace storefront, durable cart, authoritative checkout and Seller Studio | implemented | Public catalog/product/cart/checkout routes use the PostgreSQL `MarketplaceCart` aggregate with optimistic concurrency. Server-side checkout revalidates authoritative product/pricing snapshots, groups orders, uses protected Economy settlement or durable Stripe PaymentIntents, and keeps fulfillment webhook-authoritative. Seller Studio exposes owner-scoped product, append-only pricing and publication operations. |
| SumSub WebSDK, Stripe Payment Element and Google Ad Manager rewarded web adapters | implemented | SumSub loads its browser SDK only after a short-lived backend token and supports token renewal; Stripe uses `@stripe/react-stripe-js` Payment Elements for top-ups and orders while fulfillment waits for durable webhook/reconciliation; the Google Publisher Tag adapter requires explicit consent, permits one active slot and records browser callbacks as deferred evidence only. Credentials and live certification remain `blocked-by-configuration`. |
| Economy operational consoles | implemented | Permission-guarded routes under `apps/web/src/app/[locale]/(dashboards)/console/economy` cover readiness, payout review/operations, risk, Financial Crime, Trust/Safety, policies, reserve/custody, ledger/anchors, kill switches, AdRewards, Marketplace, Bounties, Treasury and legacy migration. Each surface reads real tenant-scoped APIs and exposes only its supported administrative commands. |
| Economy web localization, accessibility, capability guards, adaptive polling and step-up MFA UX | implemented | `en-US` and `pt-BR` message trees have enforced parity; layouts apply server-side capability guards; controls use labelled UI primitives and keyboard-safe forms; `EconomyAdaptiveRefresh` pauses for hidden/dirty pages and backs off after failures; receipt-protected console actions bind a one-use step-up challenge to their canonical operation payload. |
| Unit, PostgreSQL and provider-contract suites enforce no skips and 100% backend Economy line, branch and method coverage | implemented | `economy-gate.sh` enforces 100% for all three metrics and rejects skipped/zero-test TRX. The 2026-08-30 merged PR profile passed migrations, all nine Economy/compliance assemblies and provider contracts with zero skips and 100% line, branch and method coverage. |
| Full web unit, OpenAPI/client, production build and browser E2E acceptance matrix | implemented | `apps/web/vitest.economy.config.mts` enforces 100% statements, branches, functions and lines across the changed Economy, Marketplace and Seller Studio surface. The Economy gate executes the complete web suite, locale parity, generated-client no-diff check, production build and authenticated/public browser route matrix, and rejects zero-test or skipped cases. |
| Capability activation after legal, KYC/AML, signed policy, certified provider and operational approval | blocked-by-configuration | Defined by `ADR-20260821-economy-production-rollout.md`; the global allowlist starts empty |
| Stripe Connect account credentials, external KYC/AML registrations and WORM storage credentials | blocked-by-configuration | Must be supplied and certified outside source control before activation |

## Evidence gates

- Repository policy: `scripts/ci/verify-repository-policy.sh` writes
  `artifacts/test-results/repository-policy/preflight-summary.txt` for both
  success and failure.
- Economy: `scripts/ci/verify-economy.sh` writes
  `artifacts/test-results/economy/preflight-summary.txt` on every exit path,
  with per-stage logs, TRX, coverage, OpenAPI, Vitest, Playwright and container
  evidence under the same Economy-specific artifact root.
- The release checklist must verify remote branch protection for both required
  gates and must reject any skipped Docker or payout suites.
- External sandbox/live-provider certification is release evidence supplied by
  operations; its absence keeps the corresponding capability disabled and does
  not convert an implemented provider adapter into an activation default.
